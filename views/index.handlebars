<div class="row">
  <div class="col-xs-2 col-sm-3 col-md-4 uneaten">
      <h3>Burgers to Eat</h3>
    <form action="/{{this.id}}?_method=DELETE" method="POST">
      {{#each burgers}}
        
          <p>{{this.id}}. {{this.burger_name}}  <button type="submit" class="btn btn-warning btn-xs" id="devour">Devour</button></p>        
        
      {{/each}}
    </form>
  </div>
  <div class="col-xs-8 col-sm-6 col-md-4" id="sign">
  		<img src="assets/hamburger.png">
  		<h1>Eat-Da-Burger!</h1></div>
  <div class="col-xs-2 col-sm-3 col-md-4"></div>
</div>

<div class="row">
  <div class="col-xs-2 col-sm-3 col-md-4">   
  </div>
  <div class="col-xs-8 col-sm-6 col-md-4" id="addBurger">
  	<form method="POST" action="/create">
  		<p>Enter a type of burger you would like to eat & click 'Add Burger'.</p>
  		<textarea class="form-control" rows="2"></textarea>
		  <button type="submit" class="btn btn-danger" id="submit">Add Burger</button>
	  </form>	
  </div>
  
  <div class="col-xs-2 col-sm-3 col-md-4">

      <form action="/{{this.id}}?_method=DELETE" method="POST">
  <!-- option 2, pass in the id of the cat inside the form - you would access this in the route using req.body.id not req.params.id -->
  <!-- <input type="hidden" name="id" value="{{this.id}}"> -->
  <button type="submit">DEVOUR!</button>
</form>
    
  </div>
</div>